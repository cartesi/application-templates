{
  "name": "my-dapp",
  "version": "0.1.0",
  "description": "JS DApp",
  "type": "module",
  "dependencies": {
    "@cartesi/rollup": "^1.0.0-alpha.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "esbuild ./src/index.js --bundle --outfile=dist/index.mjs --platform=node --format=esm --target=node22 --packages=external",
    "start": "node src/index.js"
  },
  "keywords": [
    "cartesi"
  ],
  "license": "MIT"
}
